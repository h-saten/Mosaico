using Autofac;

namespace Mosaico.Domain.ProjectManagement
{
    public class ProjectManagementModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}