import { ProjectTitleDirective } from './project-title.directive';

describe('ProjectTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectTitleDirective(null);
    expect(directive).toBeTruthy();
  });
});
