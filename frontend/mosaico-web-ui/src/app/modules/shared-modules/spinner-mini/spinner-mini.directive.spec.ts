import { SpinnerMiniDirective } from './spinner-mini.directive';

describe('SpinnerMiniDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinnerMiniDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
