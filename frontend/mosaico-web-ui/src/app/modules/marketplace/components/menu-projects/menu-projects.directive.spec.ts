import { ElementRef } from '@angular/core';
import { MenuProjectsDirective } from './menu-projects.directive';

describe('MenuProjectsDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuProjectsDirective();
    expect(directive).toBeTruthy();
  });
});
